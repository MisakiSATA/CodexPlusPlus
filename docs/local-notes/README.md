# 本地工作记录（local notes）

这里收纳的是各次修复会话（Codex / Claude）产出的状态报告、验证指南和临时脚本，
只作为历史记录保留，不是项目文档，也不参与构建：

- `*.md`：2026-09-01 至 2026-09-04 期间自定义模型、推理强度、catalog 缓存等修复的过程记录。
- `scratch/`：当时用来手工验证的临时脚本与代码片段（不属于任何 crate）。

正式文档见上一级 `docs/` 目录与仓库根目录的 `README.md`。
